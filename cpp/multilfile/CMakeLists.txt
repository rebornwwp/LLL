# https://www.learncpp.com/cpp-tutorial/header-files/ 1. compile add.cpp and
# main.cpp into *.o file 2. then user linker to link all obj file to a
# executable binary file.
add_executable(multilfile main.cpp add.cpp base.cpp)
